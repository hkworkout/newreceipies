<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
       <%= link_to "MY LOGO", root_path, class: "navbar-brand btn btn-lg btn-success" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav">
        <li> <span><%= link_to "link2","", class: "btn btn-warning navbar-btn" %></span></li>
        <li> <span><%= link_to "link3","", class: "btn btn-warning navbar-btn" %></span></li>
        <% if logged_in? %>
          <li> <span><%= link_to "New Recipe",new_recipe_path, class: "btn btn-warning navbar-btn" %></span></li>
        <% end %>
      </ul>

      <% if logged_in? %>
        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <%= link_to "#", class: "dropdown-toggle", "data-toggle" => "dropdown"  do %>
             <%= current_user.chefname %> <b class="caret"></b>
            <% end %>
            <ul class="dropdown-menu">
              <li><%= link_to "View Profile",chef_path(current_user) %></li>
              <li><%= link_to "Edit Profile",edit_chef_path(current_user) %></li>
              <li class="divider"></li>
              <li><%= link_to "Logout",logout_path, class: "glyphicon glyphicon-log-out" %></li>
            </ul>
          </li>
        </ul>
        <% else %>
          <ul class="nav navbar-nav navbar-right">
            <li> <%= link_to "Singin",login_path, class: "glyphicon glyphicon-log-in" %></li>
          </ul>
      <% end %>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>